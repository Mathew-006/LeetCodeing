class MyStack {
    ArrayDeque<Integer> q1;
    int size;

    public MyStack() {
        q1 = new ArrayDeque<>();
        size = 0;
    }
    
    public void push(int x) {
        q1.offerLast(x);
        size++;
    }
    
    public int pop() {
        for(int i = 0; i < size - 1; i++) {
            int first = q1.pollFirst();
            q1.offerLast(first);
        }

        size--;
        return q1.pollFirst();
    }
    
    public int top() {
        int top = 0;
        for(int i = 0; i < size; i++) {
            top = q1.pollFirst();
            q1.offerLast(top);
        }

        return top;
    }
    
    public boolean empty() {
       // Clean, direct boolean evaluation
       return size == 0;
    }
}

